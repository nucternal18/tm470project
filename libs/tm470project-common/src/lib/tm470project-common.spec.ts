import { tm470projectCommon } from './tm470project-common';

describe('tm470projectCommon', () => {
  it('should work', () => {
    expect(tm470projectCommon()).toEqual('tm470project-common');
  });
});
